# RecipeHomeFragment

Android Studio project for the "Home" recipe screen.

The main screen is implemented as `HomeFragment` and opened from `MainActivity`.
It contains:

- Search field
- Recipe categories
- Two-column recipe list
- Recipe cards with author, image area, title, category, time, and favorite button

Open the `RecipeHomeFragment` folder in Android Studio and run the `app` configuration.
